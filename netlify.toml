[functions]
  node_bundler = "esbuild"
  # Include ejs as an external module so bundling/serving functions have it available
  external_node_modules = ["ejs"]
  included_files = ["public/**", "dist/**"]
[[redirects]]
  from = "/schedule"
  to = "/public/schedule.html"
  status = 200
  force = true

[[redirects]]
  from = "/images/*"
  to = "/images/:splat"
  status = 200
  force = true

  # Fallback: route everything else to the main website function
[[redirects]]
  force = true
  from = "/*"
  status = 200
  to = "/.netlify/functions/website/:splat"

[[plugins]]
  package = "@netlify/plugin-emails"